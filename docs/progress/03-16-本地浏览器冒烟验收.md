# 阶段 4 / 节点 16：本地真实浏览器冒烟验收

## 状态

本地游客核心流程通过；CI 自动化和真实供应商流程仍待补齐。

## 环境

- 日期：2026-09-23（Asia/Shanghai）
- 应用：`http://127.0.0.1:5173`
- 浏览器：Microsoft Edge，由 Playwright CLI 驱动
- 身份：游客
- 外部凭据：未配置

## 已验证流程

1. 从官网“游客直接使用”进入本地画板，显示“游客 · 仅本机”。
2. 在 Excalidraw 创建文字题目 `2x + 3 = 11`。
3. 取消选择后再次点击题目，显示“解题（Solve）”和“提示（Hint）”菜单。
4. 点击 Solve 后页面显示“已准备 1 个元素的解题输入 · 11 个文字”。
5. 刷新同一路由，题目从 IndexedDB 恢复；再次点击仍可唤起 AI 菜单。
6. 打开本地数据面板，可看到当前画板记录。
7. 下载完整备份，文件解析结果为 `format: easy-to-learn`、`exportVersion: 1`、Canvas `schemaVersion: 2`，包含 1 个文字元素且无资产。
8. 全流程浏览器控制台无 error 或 warning。

## 未计为通过

- 本地 Vite 服务没有 Supabase、Upstash Redis 和 Gemini 凭据，不能验证真实 AI 返回、配额 cookie、反馈提交或供应商错误处理。
- 没有登录账号和远端项目，不能验证 OAuth、RLS、Storage、云同步与版本冲突 E2E。
- 本节点按 `playwright` skill 使用 CLI 进行真实浏览器验证，没有新增测试文件；它不满足生产完成标准中的“关键流程有自动化 E2E”，该项仍保持开放。
